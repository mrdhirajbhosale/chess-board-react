const todos = (state: any, action: any) => {
  switch (action.type) {
    case 'PRE':
      return {
        
      }
    case 'NEXT':
      return {
        
      }
    default:
      return state
  }
}

export default todos;
